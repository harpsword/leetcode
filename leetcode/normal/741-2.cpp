#include <iostream>
#include <vector>
#include <set>
#include<algorithm>

using namespace std;

class Solution {
public:
    int cherryPickup(vector<vector<int>>& grid) {
        int n = grid.size();

    }

    int dp(vector<vector<int>>& grid, int x1, int x2, int y1){
        int y2 = x1 + y1 - x2;
        if (x1 < 0 || x1 >= )
        if (x1==x2 && y1 == y2){
            if (grid[x1][y1]==-1) return -1;

        } 
    }
};